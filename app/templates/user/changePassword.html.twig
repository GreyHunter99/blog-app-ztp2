{% include 'default/_form.html.twig' with {
    page_title: 'title_change_password',
    page_title_param: {'%id%': user.id},
    attr: {
        method: 'PUT',
        action: url('user_changePassword', {'id': user.id}),
    },
    form: form,
    submit_label: 'action_change'|trans,
    back_to_user: 'user_show',
} %}