{% include 'default/_form.html.twig' with {
    page_title: 'title_revoke_admin',
    page_title_param: {'%id%': user.id},
    attr: {
        method: 'PUT',
        action: url('user_grantAdmin', {'id': user.id}),
    },
    form: form,
    submit_label: 'action_revoke'|trans,
    back_to_user: 'user_show',
} %}